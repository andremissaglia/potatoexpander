#include "Image.h"

void compress(FILE *input, FILE *output){
        /*BMPImage *img= readBMP(input);
        writeBMP(output,img);
        freeBMP(img);*/
       /* Huffman * h = newHuffman();
        writechar(h, 'a');
        writechar(h, 'a');
        writechar(h, 'a');
        writechar(h, 'b');
        writechar(h, 'c');
        writeall(h);*/
        
}

